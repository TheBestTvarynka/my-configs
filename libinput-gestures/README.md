
# Instalation

```bash
# install dependencies
paru -S libinput-gestures
sudo pacman -S xdotool

# copy config file from this repo to the /etc/libinput-gestures.conf

# autostart: https://github.com/bulletmark/libinput-gestures#configuration
libinput-gestures-setup autostart start
```

